function dientich(a, b) { //gọi tên hàm
    //let a = 6; //khai báo biến, có thể bỏ dòng này và tự thêm biến ở phần gọi hàm
    //let b = 8;
    console.log(`Diện tích hình chữ nhật bằng dài a: ${a} x rộng b: ${b} = ${a * b}`); // dòng này để khai báo hàm
}
dientich(); //dòng này để gọi hàm
dientich(4, 5); //gọi hàm thêm biến riêng